# Operating-System-Design-and-Implementation

The whole implementation of a micro-kernel on Raspberry Pi 3B+, including 

(1) relocatable bootloader

(2) exception/interrupt handling

(3) buddy system memory management

(4) multiprocess support with physical address/virtual address

(5) VFS/filesystem


One can demo it on QEMU system or real hardware.
